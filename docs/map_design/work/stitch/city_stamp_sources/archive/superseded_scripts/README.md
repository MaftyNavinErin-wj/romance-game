# Superseded Scripts

These scripts are retained for audit history only.

- `create_primary_object_cutout_v7.py`: generated rejected hand-guided primary `v7` / `v8` attempts.
- `create_primary_rembg_cutout_v9.py`: one-off primary-only rembg proof script, superseded by top-level `create_city_stamp_rembg_cutouts_v9.py`.

Use `../../create_city_stamp_rembg_cutouts_v9.py` for the current v9 source proof set.
